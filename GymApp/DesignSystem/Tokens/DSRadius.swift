import Foundation

// MARK: - DSRadius

enum DSRadius {
    /// 8pt — subtle rounding
    static let sm: CGFloat = 8
    /// 12pt — standard cards and containers
    static let md: CGFloat = 12
    /// 16pt — prominent elements
    static let lg: CGFloat = 16
    /// 9999pt — pill shape
    static let full: CGFloat = 9999
}
